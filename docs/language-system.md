# 🌐 Language System

## Current Language

We use a variable:

let currentLanguage = "en";

## Switching Language

* User clicks language button
* Toggle between "en" and "mm"
* Update all text dynamically

## Implementation

Text is stored in two versions:

* English (en)
* Myanmar (mm)

The UI updates based on `currentLanguage`.
